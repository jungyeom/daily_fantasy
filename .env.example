# Daily Fantasy Environment Variables
# Copy this file to .env and fill in your values
# .env is gitignored and will not be committed

# Yahoo DFS Credentials
DFS_YAHOO_USERNAME=your_yahoo_email@example.com
DFS_YAHOO_PASSWORD=your_yahoo_password

# FanDuel API Tokens (for read-only data collection)
# To get these tokens:
# 1. Log into FanDuel DFS (https://www.fanduel.com/contests)
# 2. Open browser dev tools (F12) -> Network tab
# 3. Refresh the page and find any request to api.fanduel.com
# 4. Copy the 'Authorization' header value (starts with "Basic ...")
# 5. Copy the 'X-Auth-Token' header value
FANDUEL_AUTH_TOKEN=Basic your_base64_encoded_token
FANDUEL_SESSION_TOKEN=your_session_token

# Email Notifications (optional)
DFS_SENDGRID_API_KEY=your_sendgrid_api_key
DFS_EMAIL_TO=your_email@example.com
